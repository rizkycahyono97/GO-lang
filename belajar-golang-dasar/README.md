# Belajar Golang Dasar

Proyek ini berisi kumpulan contoh kode untuk memahami dasar-dasar bahasa pemrograman Go.

## Struktur Folder

```
.
├── anonymous_function.go       # Contoh fungsi anonim di Go
├── any.go                      # Penggunaan tipe data any (interface kosong)
├── array.go                    # Contoh penggunaan array di Go
├── asterisk_operator.go        # Operator asterisk (*) dalam Go
├── boolean.go                  # Tipe data boolean dan operasinya
├── break.go                    # Penggunaan pernyataan break dalam perulangan
├── closure.go                  # Contoh closure di Go
├── constant.go                 # Cara mendeklarasikan konstanta
├── continue.go                 # Penggunaan pernyataan continue dalam perulangan
├── conversion.go               # Konversi tipe data di Go
├── database                    # Folder untuk kode terkait database
│   └── database.go             # Contoh koneksi dan operasi database
├── defer.go                    # Penggunaan defer dalam Go
├── error_custom.go             # Cara membuat custom error
├── error.go                    # Penanganan error di Go
├── for.go                      # Contoh perulangan for di Go
├── function_as_parameter.go    # Fungsi sebagai parameter
├── function.go                 # Definisi dan pemanggilan fungsi di Go
├── function_parameter.go       # Parameter dalam fungsi
├── function_return.go          # Penggunaan return dalam fungsi
├── function_value.go           # Fungsi sebagai nilai
├── go.mod                      # File modul Go
├── helloworld.go               # Program Hello World sederhana
├── helper                      # Folder untuk fungsi-fungsi pembantu
│   └── helper.go               # Contoh fungsi helper
├── if.go                       # Penggunaan if statement
├── import.go                   # Cara mengimpor package di Go
├── init.go                     # Fungsi init di Go
├── interface.go                # Contoh penggunaan interface
├── internal                    # Folder internal untuk kode modular
│   └── internal.go             # Contoh kode internal
├── map.go                      # Contoh penggunaan map di Go
├── matematika.go               # Operasi matematika di Go
├── named_return_values.go      # Return values dengan nama
├── nil.go                      # Nil dalam Go
├── number.go                   # Tipe data numerik di Go
├── operasi_boolean.go          # Operasi boolean
├── panic.go                    # Contoh penggunaan panic
├── perbandingan.go             # Operasi perbandingan
├── pointer_function.go         # Pointer dalam fungsi
├── pointer.go                  # Penggunaan pointer
├── pointer_method.go           # Metode dengan pointer receiver
├── README.md                   # Dokumentasi proyek ini
├── recursive_function.go       # Fungsi rekursif
├── return_multiple_values.go   # Fungsi dengan multiple return
├── slice.go                    # Contoh penggunaan slice di Go
├── string.go                   # Operasi string
├── struct.go                   # Penggunaan struct di Go
├── swtich.go                   # Switch case di Go
├── type_assertions.go          # Type assertions dalam Go
├── type_declaration.go         # Deklarasi tipe baru
├── variable.go                 # Deklarasi dan penggunaan variabel
└── variadic_function.go        # Fungsi dengan parameter variadic
```

## Cara Menggunakan

1.  **Instal Go** jika belum terpasang di sistem.
2.  Clone atau download repository ini.
3.  Jalankan setiap file dengan perintah:
    
    ```sh
    go run *nama_file.go*
    ```
    
4.  Untuk mengelola modul, gunakan perintah:
    
    ```sh
    go mod tidy
    ```
    

## Kontribusi

Jika ingin menambahkan contoh baru atau memperbaiki kode, silakan buat pull request atau buka issue.

* * *

**Selamat belajar Golang Dasar! 🚀**